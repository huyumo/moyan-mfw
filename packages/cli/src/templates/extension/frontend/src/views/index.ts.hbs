/**
 * @fileoverview {{displayName}}扩展包菜单树配置
 * @description 定义扩展包的页面菜单树（含内联组件引用）。
 *
 * 新增页面时在此处添加菜单节点，并在节点上内联 component 字段。
 */
import type { FrontendAppTypeMenuConfig } from 'moyan-mfw-base/frontend'

/**
 * {{displayName}}扩展包菜单树
 *
 * appTypeCode 使用 _ext_{{name}} 前缀，与业务层 AppType 区分。
 */
export const {{camelCase name}}MenuTrees: FrontendAppTypeMenuConfig[] = [
  {
    appTypeCode: '_ext_{{name}}',
    label: '{{displayName}}',
    icon: 'Menu',
    order: 50,
    children: [
      // 在此添加页面节点，例如：
      // {
      //   path: 'demo',
      //   name: '示例页面',
      //   icon: 'Document',
      //   component: () => import('./views/demo/Index.vue'),
      //   permissions: ['添加', '编辑', '删除'],
      // },
    ],
  },
]
