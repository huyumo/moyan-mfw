/**
 * @fileoverview Swagger SDK 生成脚本
 * @description 后端运行后执行 pnpm build:api 从 Swagger 自动生成 API 调用类
 *   一期使用手动编写的 apis/ledger/index.ts，后续可切换为自动生成
 */
// eslint-disable-next-line no-console
console.log('api.build.cjs: 一期使用手动 API 调用类，后续可通过 moyan-api 从 Swagger 自动生成')
