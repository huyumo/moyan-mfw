---
task: 种子数据与权限菜单修复
status: completed
priority: P0
started: 2026-04-07
completed: 2026-04-07 18:10
assignee: @ai
---

## 任务概述

修复种子数据和应用初始化问题，确保权限系统 API 返回正确数据。

## 问题列表

| 序号 | 问题 | 严重性 | 状态 |
|------|------|--------|------|
| 1 | `/api/auth/apps` 返回空数组 | P0 | ✅ 已修复 |
| 2 | `/api/auth/permissions` 返回空菜单 | P0 | ✅ 已修复 |
| 3 | 种子数据 permCode 唯一键冲突 | P1 | ✅ 已修复 |
| 4 | UserPermissionsDto 验证失败 | P1 | ✅ 已修复 |
| 5 | `/api/permissions/tree/all` BigInt 序列化错误 | P0 | ✅ 已修复 |
| 6 | `/api/auth/permissions` 返回 NORMAL 权限类型 | P0 | ✅ 已修复 |
| 7 | `filterVisibleNodes` 过滤逻辑导致菜单为空 | P0 | ✅ 已修复 |
| 8 | 种子数据缺少权限管理 PC 权限 | P0 | ✅ 已修复 |

## 修复详情

### 1. 应用实例初始化缺失

**问题**：种子数据没有创建应用实例，导致 `/api/auth/apps` 返回空数组。

**修复**：在 `packages/base-backend/src/database/seeds/index.ts` 中添加 `seedAppInstances()` 函数。

**代码变更**：
```typescript
async function seedAppInstances(dataSource: DataSource): Promise<void> {
  const systemAppType = await dataSource.manager.findOne(AppType, { 
    where: { typeCode: 'system' } 
  });
  const adminUser = await dataSource.manager.findOne(User, { 
    where: { username: 'admin' } 
  });
  
  const appInstances = [{
    appName: '系统管理后台',
    appCode: 'system-admin',
    appTypeId: systemAppType.id,
    ownerId: adminUser.id,
    appStatus: 1,
  }];
  
  // 创建应用实例
  for (const app of appInstances) {
    const existing = await dataSource.manager.findOne(AppInstance, {
      where: { appCode: app.appCode },
    });
    if (!existing) {
      await dataSource.manager.save(AppInstance, app);
    }
  }
}
```

**执行顺序调整**：
```
AppTypes → Permissions → Roles → AdminUser → AppInstances → RolePermissions → UserRoles
```

---

### 2. 全局角色识别失败

**问题**：`/api/auth/permissions` 返回空菜单，全局角色（null appId, null appTypeId）未被识别。

**修复**：在 `auth.service.ts` 的 `getUserPermissions()` 方法中添加 `isGlobalRole` 判断。

**代码变更**：
```typescript
userRoles.forEach((ur) => {
  if (ur.role) {
    const isGlobalRole = !ur.role.appId && !ur.role.appTypeId;
    const isAppRole = ur.role.appId === appId;
    const isAppTypeRole = ur.role.appTypeId === appTypeId;
    if (isGlobalRole || isAppRole || isAppTypeRole) {
      roleIds.push(ur.roleId);
    }
  }
});
```

---

### 3. 种子数据唯一键冲突

**问题**：`permCode` 有 `@Unique` 约束，但 `findOne` 检查包含 `permissionType` 条件，导致重复数据插入失败。

**修复**：移除 `permissionType` 条件，添加 `try-catch` 处理冲突。

**代码变更**：
```typescript
// 检查权限编码是否存在（移除 permissionType 条件）
const existingPermission = await this.permissionRepository.findOne({
  where: { permCode: fullPermCode },
});

if (existingPermission) {
  throw new ConflictException(`权限编码已存在：${fullPermCode}`);
}
```

---

### 4. DTO 验证缺失

**问题**：`UserPermissionsDto` 的 `appId` 字段缺少 `@IsUUID()` 验证。

**修复**：在 `user-permissions.dto.ts` 中添加验证装饰器。

**代码变更**：
```typescript
@IsUUID()
@ApiProperty({ description: '应用实例 ID', example: 'app-instance-uuid' })
appId: string;
```

---

### 5. BigInt 序列化错误

**问题**：`/api/permissions/tree/all` 返回 `Cannot mix BigInt and other types` 错误。

**根本原因**：TypeORM 的 `bigint` 列在 MySQL 驱动中返回**字符串**，而非 JavaScript BigInt 对象。

**修复位置**：`packages/base-backend/src/common/guards/permission.guard.ts:92-101`

**代码变更**：
```typescript
// 构建用户权限映射：permCode → permissionValue (位运算合并)
const userPermissionMap = new Map<string, bigint>();
for (const rp of userRolePermissions) {
  if (rp.permission && rp.permission.permStatus === 1) {
    const permCode = rp.permission.permCode;
    // TypeORM 返回的 permissionValue 可能是字符串，需要转换为 BigInt
    const rpPermValue = typeof rp.permissionValue === 'string'
      ? BigInt(rp.permissionValue)
      : (rp.permissionValue as bigint);
    const existing = userPermissionMap.get(permCode);
    if (existing) {
      userPermissionMap.set(permCode, existing | rpPermValue);
    } else {
      userPermissionMap.set(permCode, rpPermValue);
    }
  }
}
```

**伴随修复**：
- `all-exceptions.filter.ts`：`message` 类型改为 `string | bigint`
- `transform.interceptor.ts`：修复响应类型定义

---

### 6. menuTree 返回 NORMAL 权限类型

**问题**：system 应用类型（PC 权限）的菜单中包含了 `permissionType: "NORMAL"` 的数据（如 `business:view`）。

**根本原因**：
1. 种子数据中 admin 角色绑定了所有权限（包括 PC 和 NORMAL）
2. `getUserPermissions` 方法没有根据应用类型过滤权限类型

**修复位置**：`packages/base-backend/src/modules/sys/auth/auth.service.ts`

**代码变更**：
```typescript
// 添加 AppTypePermissionEntity 依赖
@InjectRepository(AppTypePermissionEntity)
private appTypePermissionRepository: Repository<AppTypePermissionEntity>,

// 根据应用类型决定允许的权限类型
const allowedPermissionType = appType?.typeCode === 'system'
  ? PermissionType.PC
  : PermissionType.NORMAL;

rolePermissions.forEach((rp) => {
  if (rp.permission && rp.permission.permStatus === 1) {
    // 过滤：权限类型必须匹配
    if (rp.permission.permissionType !== allowedPermissionType) {
      return;
    }
    // ... 其他过滤逻辑
  }
});
```

**伴随修改**：
- `auth.module.ts`：添加 `AppTypePermissionEntity` 到 `forFeature` 导入列表

---

### 7. filterVisibleNodes 过滤逻辑修复

**问题**：`pc_root` 根节点 `isVisible=0`，导致所有子节点被过滤掉，menuTree 返回空数组。

**修复位置**：`packages/base-backend/src/modules/sys/auth/auth.service.ts:559-571`

**代码变更**：
```typescript
private filterVisibleNodes(nodes: PermissionTreeNodeDto[]): PermissionTreeNodeDto[] {
  const result: PermissionTreeNodeDto[] = [];
  for (const node of nodes) {
    if (node.isVisible === 1) {
      // 当前节点可见，保留
      const newNode = { ...node };
      if (node.children && node.children.length > 0) {
        newNode.children = this.filterVisibleNodes(node.children);
      }
      result.push(newNode);
    } else {
      // 当前节点不可见，递归处理子节点（子节点提升为根节点）
      if (node.children && node.children.length > 0) {
        const visibleChildren = this.filterVisibleNodes(node.children);
        result.push(...visibleChildren);
      }
    }
  }
  return result;
}
```

**验证结果**：
```bash
GET /api/auth/permissions?appId=1cc0411e-3d62-491c-9746-ee7d5282401c
{
  "menuTree": [
    {"permName":"首页","permCode":"pc_root:dashboard",...},
    {"permName":"系统管理","permCode":"pc:system",...,"children":[
      {"permName":"用户管理","permCode":"pc:system:user",...,"children":[
        {"permName":"用户列表","permCode":"pc:system:user:list",...}
      ]}
    ]}
  ],
  "permissions": ["pc_root:business:reports", ...],
  "appTypeId": "f7eecf8d-0576-4935-bbc9-ca22b9b4bb9f"
}
# menuTree 正确显示，pc_root 被过滤但其子节点提升为根节点
# 所有权限都是 PC 类型，不再包含 NORMAL 权限
```

---

### 8. 种子数据缺少权限管理 PC 权限

**问题**：权限管理接口需要 `pc_root:sys:permission` 和 `pc_root:sys:permission-pc` 权限，但种子数据没有创建这些权限，导致超级管理员没有权限访问权限管理模块。

**根本原因**：
1. 种子数据只创建了 `pc:system`、`pc:system:user`、`pc:system:user:list` 几个 PC 权限
2. 权限管理相关的 PC 权限（`pc_root:sys:permission`、`pc_root:sys:permission-pc`）是通过 `syncPermissions` 接口同步路由时自动创建的（`isAutoSync=1`）
3. 种子数据执行时，超级管理员绑定了当时所有权限，但不包括后续同步创建的权限

**修复位置**：`packages/base-backend/src/database/seeds/index.ts`

**修复代码**：
```typescript
const pcPermissions = [
  // ... 其他权限
  // 权限管理相关权限（用于权限管理接口的权限控制）
  {
    permName: '系统管理',
    permCode: 'pc_root:sys',
    nodeType: NodeType.MENU,
    routePath: '/sys',
    iconName: '',
  },
  {
    permName: '权限管理',
    permCode: 'pc_root:sys:permission',
    nodeType: NodeType.PAGE,
    routePath: '/sys/permission',
    iconName: '',
    permissionValue: buildPerValue(['查看', '添加', '编辑', '删除', '导出', '导入']), // 63n
  },
  {
    permName: 'PC 权限管理',
    permCode: 'pc_root:sys:permission-pc',
    nodeType: NodeType.PAGE,
    routePath: '/sys/permission-pc',
    iconName: '',
    permissionValue: buildPerValue(['查看', '添加', '编辑', '删除', '导出', '导入']), // 63n
  },
];
```

**验证结果**：
```bash
# 种子数据执行
pnpm ts-node src/database/run-seeds.ts
# 输出：✓ 超级管理员绑定 22 个权限

# 权限管理接口查询
GET /api/permissions?permCode=pc_root:sys:permission
{"code":0,"data":{"list":[{"permName":"权限管理","permCode":"pc_root:sys:permission","permissionValue":"63",...}],"total":2}}

# 创建权限接口
POST /api/permissions
{"code":0,"data":{"permName":"测试权限","permCode":"pc_root:test",...},"message":"创建成功"}
```

---

## 验证结果

```bash
# 1. 应用列表 API
GET /api/auth/apps
{"code":0,"data":{"apps":[{"appId":"1cc0411e-3d62-491c-9746-ee7d5282401c","appName":"系统管理后台",...}]},"message":"获取成功"}

# 2. 权限菜单 API
GET /api/auth/permissions?appId=1cc0411e-3d62-491c-9746-ee7d5282401c
{"code":0,"data":{"menuTree":[...],"permissions":[22 个权限编码],...},"message":"获取成功"}

# 3. 权限树 API
GET /api/permissions/tree/all?permissionType=PC
{"code":0,"data":[{"id":"e7b01801-9008-470e-9c1d-88559999b395","permName":"PC 权限根节点",...}],"message":"查询成功"}

# 4. 构建验证
pnpm run build → ✅ 成功
pnpm typecheck → ✅ 无错误
```

---

## 修改文件清单

1. `packages/base-backend/src/database/seeds/index.ts` - 添加应用实例初始化 + 权限管理 PC 权限
2. `packages/base-backend/src/modules/sys/auth/auth.service.ts` - 全局角色识别逻辑 + 权限类型过滤 + 可见性节点过滤
3. `packages/base-backend/src/modules/sys/auth/auth.module.ts` - 添加 AppTypePermissionEntity 依赖
4. `packages/base-backend/src/modules/sys/auth/dto/req/user-permissions.dto.ts` - 添加 @IsUUID()
5. `packages/base-backend/src/common/guards/permission.guard.ts` - BigInt 类型转换
6. `packages/base-backend/src/common/filters/all-exceptions.filter.ts` - 类型修复
7. `packages/base-backend/src/common/interceptors/transform.interceptor.ts` - 类型修复
8. `.claude/TASK.md` - 任务文档更新
9. `docs/04-项目实施/05-任务追踪/2026-04-07-1800-种子数据与权限菜单修复.md` - 任务详情文档

---

## 关键决策

- **BigInt 处理方案**：选择在使用方（PermissionGuard）进行类型转换，而非全局 JSON 序列化拦截
- **种子数据执行顺序**：确保依赖关系正确（先有用户，再创建应用实例）
- **全局角色定义**：`appId === null && appTypeId === null` 为全局角色，适用于所有应用

---

## 交付物

- ✅ 所有 API 接口恢复正常
- ✅ 构建和类型检查通过
- ✅ 任务文档归档完成

## 备注

种子数据执行后创建的应用实例 ID：`1cc0411e-3d62-491c-9746-ee7d5282401c`
