# 2026-04-14-001 权限池接口统一优化方案 - 需求对齐会议

## 任务信息

| 项目 | 内容 |
|------|------|
| 任务 ID | 2026-04-14-001 |
| 任务名称 | 权限池接口统一优化方案 - 需求对齐会议 |
| 优先级 | P0 |
| 负责人 | PM-Agent |
| 参与者 | Frontend-Dev, Backend-Dev, QA-Agent |
| 创建日期 | 2026-04-14 |
| 截止日期 | 2026-04-16 |
| 状态 | 进行中 |

---

## 背景与目标

### 背景

当前权限池配置和角色权限分配两个功能使用相似的接口设计，但返回数据结构存在字段命名不一致的问题：
- 权限池接口返回体使用 `inPool: boolean` 字段
- 角色权限接口返回体使用 `assigned: boolean` 字段

这导致前端需要编写两套相似但不同的处理逻辑，增加维护成本。

### 目标

通过需求对齐会议，确保各方对优化方案达成一致，明确实施范围、技术方案和排期。

---

## 参考文档

- [权限池与角色权限接口统一优化方案](../../01-业务需求/01-基础设施/06-API%20接口/2026-04-14_权限池与角色权限接口统一优化方案.md)

---

## 会议议程

### 1. 介绍权限池与角色权限接口统一优化方案

**讨论要点**：
- [ ] 方案背景说明（当前问题）
- [ ] 优化目标（统一字段命名）
- [ ] 影响范围评估

**核心变更**：
| 接口 | 变更前 | 变更后 |
|------|--------|--------|
| 权限池 GET | `{ inPool: boolean }` | `{ checked: boolean }` |
| 角色权限 GET | `{ assigned: boolean }` | `{ checked: boolean }` |

---

### 2. 确认数据结构变更

**讨论要点**：
- [ ] `checked` 字段统一 - 返回体与请求体保持一致
- [ ] `parentPermissionValue` 新增 - 用于子集验证
- [ ] 类型定义更新范围

**新增字段说明**：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `checked` | boolean | 统一选中状态字段（替代 inPool/assigned） |
| `parentPermissionValue` | string? | 父层位运算权限值，用于子集验证 |

**待确认问题**：
- [ ] 是否需要过渡期同时返回旧字段？
- [ ] `parentPermissionValue` 的计算逻辑由哪方实现？

---

### 3. 后端修改范围确认

**讨论要点**：
- [ ] DTO 修改清单确认
- [ ] Service 逻辑修改范围
- [ ] 数据库是否需要变更（预计无）
- [ ] API 版本控制策略

**修改文件清单**：

| 文件路径 | 修改内容 |
|----------|----------|
| `packages/base-backend/src/modules/sys/app-type/dto/res/permission-pool-response.dto.ts` | `inPool` → `checked` |
| `packages/base-backend/src/modules/sys/role/dto/res/role-permission-response.dto.ts` | `assigned` → `checked` |
| `packages/base-backend/src/modules/sys/app-type/app-type.service.ts` | 构建 `checked` 字段逻辑 |
| `packages/base-backend/src/modules/sys/role/role.service.ts` | 构建 `checked` 字段逻辑 |

**待确认问题**：
- [ ] 后端预计开发工时？
- [ ] 是否需要新增单元测试？
- [ ] 接口文档更新由谁负责？

---

### 4. 前端修改范围确认

**讨论要点**：
- [ ] 类型定义更新
- [ ] 组件逻辑简化
- [ ] 影响的页面/组件范围

**修改文件清单**：

| 文件/组件 | 修改内容 |
|-----------|----------|
| `PermissionTreeNode` 类型 | 统一使用 `checked` 字段 |
| `MfwPermissionPoolPanel.vue` | 移除 `inPool` 字段处理逻辑 |
| 角色权限面板组件 | 移除 `assigned` 字段处理逻辑 |

**预期收益**：
- 减少约 20 行重复代码
- 类型定义减少 1 个
- 维护成本降低

**待确认问题**：
- [ ] 前端预计开发工时？
- [ ] 是否存在其他依赖此字段的逻辑？

---

### 5. 测试用例规划

**讨论要点**：
- [ ] 后端测试用例范围
- [ ] 前端测试用例范围
- [ ] E2E 测试场景
- [ ] 回归测试范围

**测试场景清单**：

| 测试场景 | 验证内容 | 负责人 |
|---------|---------|--------|
| 权限池配置读取 | 返回数据包含 `checked` 字段 | Backend-Dev |
| 权限池配置保存 | 请求数据使用 `checked` 字段 | Backend-Dev |
| 角色权限读取 | 返回数据包含 `checked` 字段 | Backend-Dev |
| 角色权限保存 | 请求数据使用 `checked` 字段 | Backend-Dev |
| 前端组件渲染 | 组件正确读取 `checked` 字段 | Frontend-Dev |
| E2E 权限池配置 | 端到端流程验证 | QA-Agent |
| E2E 角色权限配置 | 端到端流程验证 | QA-Agent |

**待确认问题**：
- [ ] 测试用例编写工时？
- [ ] 回归测试范围界定？

---

### 6. 排期确认

**讨论要点**：
- [ ] 各阶段工时评估
- [ ] 依赖关系确认
- [ ] 里程碑设定

**初步排期**：

| 阶段 | 任务 | 负责人 | 预计工时 | 预计完成 |
|------|------|--------|----------|----------|
| 阶段 1 | 后端 DTO + Service 修改 | Backend-Dev | ? | 2026-04-?? |
| 阶段 2 | 后端单元测试 | Backend-Dev | ? | 2026-04-?? |
| 阶段 3 | 前端类型 + 组件修改 | Frontend-Dev | ? | 2026-04-?? |
| 阶段 4 | 前端单元测试 | Frontend-Dev | ? | 2026-04-?? |
| 阶段 5 | E2E 测试 | QA-Agent | ? | 2026-04-?? |
| 阶段 6 | 文档更新 | PM-Agent | ? | 2026-04-?? |

**待确认问题**：
- [ ] 各角色确认可投入时间？
- [ ] 是否有其他并行任务冲突？

---

## 待确认问题清单

| 序号 | 问题 | 提出人 | 状态 | 答复人 | 答复内容 |
|------|------|--------|------|--------|----------|
| 1 | 是否需要过渡期同时返回旧字段？ | PM-Agent | 待确认 | - | - |
| 2 | `parentPermissionValue` 的计算逻辑由哪方实现？ | PM-Agent | 待确认 | - | - |
| 3 | 后端预计开发工时？ | PM-Agent | 待确认 | Backend-Dev | - |
| 4 | 前端预计开发工时？ | PM-Agent | 待确认 | Frontend-Dev | - |
| 5 | 是否存在其他依赖此字段的逻辑？ | PM-Agent | 待确认 | Frontend-Dev | - |
| 6 | 后端是否需要新增单元测试？ | PM-Agent | 待确认 | Backend-Dev | - |
| 7 | 接口文档更新由谁负责？ | PM-Agent | 待确认 | - | - |
| 8 | 测试用例编写工时？ | PM-Agent | 待确认 | QA-Agent | - |
| 9 | 回归测试范围界定？ | PM-Agent | 待确认 | QA-Agent | - |
| 10 | 各角色确认可投入时间？ | PM-Agent | 待确认 | - | - |
| 11 | 是否有其他并行任务冲突？ | PM-Agent | 待确认 | - | - |

---

## 验收标准

- [ ] 所有参与者对优化方案达成一致
- [ ] 数据结构变更范围明确
- [ ] 后端修改范围确认
- [ ] 前端修改范围确认
- [ ] 测试用例规划完成
- [ ] 排期确认并达成一致
- [ ] 待确认问题全部答复

---

## 会议纪要输出

> 会议结束后填写

**会议时间**：
**参会人员**：
**会议结论**：
**下一步行动**：

---

## 变更记录

| 日期 | 变更内容 | 变更人 |
|------|----------|--------|
| 2026-04-14 | 创建任务文档 | PM-Agent |

---

*本任务用于记录权限池接口统一优化方案需求对齐会议的全过程*