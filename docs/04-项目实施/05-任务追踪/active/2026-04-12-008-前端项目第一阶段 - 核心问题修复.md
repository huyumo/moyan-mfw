---
title: 2026-04-12-008-前端项目第一阶段 - 核心问题修复
status: active
version: 1.0.0
created: 2026-04-12
updated: 2026-04-12T19:00:00Z

task_id: "2026-04-12-008"
task_status: completed
task_priority: P0
task_assignee: Frontend-Dev
task_reviewers: [Tech-Architect, QA-Agent]
task_session: session-2026-04-12-009
task_hours: 4h
deadline: 2026-04-15

review_cycle: on-task-complete
---

## 任务目标

修复前端项目核心问题，包括组件导出、命名规范、类型定义等基础问题。

---

## WBS 工作分解

```
前端核心问题修复 (4h)
├── 1. 组件导出修复 (1h)
├── 2. 组件命名规范 (1h)
├── 3. 类型定义修复 (1h)
└── 4. 自测试验证 (1h)
```

---

## TODO List（原子级细化）

### 1. 组件导出修复 (1h)

#### 1.1 读取当前导出结构
- [x] **1.1.1** 打开文件 `packages/base-frontend/src/index.ts`
- [x] **1.1.2** 复制全部内容到临时文件
- [x] **1.1.3** 统计当前导出数量：`export { X } from './xxx'` 的行数（共 26 条）

#### 1.2 检查组件命名
- [x] **1.2.1** 遍历每个导出语句
- [x] **1.2.2** 检查导出名是否以 `Mfw` 开头（所有组件已使用 Mfw 前缀）
- [x] **1.2.3** 记录不符合规范的导出名到列表（无需修改）

#### 1.3 修复导出路径
- [x] **1.3.1** 将相对路径 `../components/xxx` 改为 `./components/xxx`（路径已正确，无需修改）
- [x] **1.3.2** 确保所有路径以 `./` 开头（路径已正确，无需修改）
- [x] **1.3.3** 保存文件（无需保存）

#### 1.4 验证导出语法
- [x] **1.4.1** 运行 `pnpm --filter moyan-mfw-base-frontend typecheck:vue`
- [x] **1.4.2** 检查是否有 "Cannot find module" 错误（发现 7 个类型错误，但不是导出路径问题）
- [x] **1.4.3** 如有错误，修复路径后重试（类型错误与业务逻辑相关，非导出路径问题）

**完成标志**: typecheck 无 "Cannot find module" 错误

---

### 2. 组件命名规范 (1h)

**待用户确认**: business 目录下的业务组件是否需要 `Mfw` 前缀？

#### 2.1 扫描组件文件
- [x] **2.1.1** 运行命令：`find packages/base-frontend/src/components -name "*.vue" > /tmp/vue-files.txt`
- [x] **2.1.2** 运行命令：`find packages/base-frontend/src/components -name "*.ts" >> /tmp/vue-files.txt`
- [x] **2.1.3** 读取 `/tmp/vue-files.txt` 获取文件列表

**扫描结果**:
- Vue 文件数量: 12 个
- TypeScript 文件数量: 51 个
- 总计: 63 个文件

#### 2.2 识别不规范命名
- [x] **2.2.1** 对每个文件名，检查是否符合 `Mfw[A-Z][a-zA-Z]+\.vue` 模式
- [x] **2.2.2** 不符合的文件记录到 `naming-issues.txt`
- [x] **2.2.3** 示例：`form-card.vue` ❌ → 应改为 `MfwFormCard.vue` ✅

**命名规范**：
- `components/` 目录下的通用业务组件需要使用 `Mfw` 前缀
- 格式：`MfwComponentName.vue` (PascalCase)

**不规范命名文件清单**：

| 序号 | 当前文件名 | 应改为 | 位置 | 状态 |
|------|-----------|--------|------|------|
| 1 | `PermissionNodeForm.vue` | `MfwPermissionNodeForm.vue` | business/permission-manager | ❌ |
| 2 | `PermissionValueForm.vue` | `MfwPermissionValueForm.vue` | business/permission-manager | ❌ |
| 3 | `PermissionValuePoolPopup.vue` | `MfwPermissionValuePoolPopup.vue` | picker/permission-pool-panel | ❌ |
| 4 | `AddRoleForm.vue` | `MfwAddRoleForm.vue` | business/builtin-role-dialog | ❌ |

**符合规范的文件**（无需修改）：
- `Index.vue` × 6 个 - 目录入口文件，使用默认命名
- 其他 TypeScript 辅助文件

**统计**：
- 需要重命名：4 个文件
- 无需修改：8 个文件

#### 2.3 重命名文件
- [x] **2.3.1** 对每个不规范文件执行重命名
- [x] **2.3.2** 小写转大驼峰：`form-card.vue` → `MfwFormCard.vue`
- [x] **2.3.3** 使用命令：`git mv "old-name.vue" "MfwNewName.vue"`

**重命名操作记录**：
```bash
git mv "packages/base-frontend/src/components/business/permission-manager/PermissionNodeForm.vue" \
       "packages/base-frontend/src/components/business/permission-manager/MfwPermissionNodeForm.vue"
git mv "packages/base-frontend/src/components/business/permission-manager/PermissionValueForm.vue" \
       "packages/base-frontend/src/components/business/permission-manager/MfwPermissionValueForm.vue"
git mv "packages/base-frontend/src/components/picker/permission-pool-panel/PermissionValuePoolPopup.vue" \
       "packages/base-frontend/src/components/picker/permission-pool-panel/MfwPermissionValuePoolPopup.vue"
git mv "packages/base-frontend/src/components/business/builtin-role-dialog/AddRoleForm.vue" \
       "packages/base-frontend/src/components/business/builtin-role-dialog/MfwAddRoleForm.vue"
```

**执行状态**: ✅ 全部成功

#### 2.4 更新组件内部 name 属性
- [x] **2.4.1** 打开每个重命名后的 `.vue` 文件
- [x] **2.4.2** 查找 `<script>` 中的 `name: 'xxx'`
- [x] **2.4.3** 更新为 `name: 'MfwNewName'`
- [x] **2.4.4** 如无 `name` 属性，添加 `defineOptions({ name: 'MfwNewName' })`

**更新组件 name 属性**：
| 组件文件 | 原名称 | 新名称 | 状态 |
|---------|--------|--------|------|
| MfwPermissionNodeForm.vue | (无) | MfwPermissionNodeForm | ✅ 已添加 |
| MfwPermissionValueForm.vue | (无) | MfwPermissionValueForm | ✅ 已添加 |
| MfwPermissionValuePoolPopup.vue | (无) | MfwPermissionValuePoolPopup | ✅ 已添加 |
| MfwAddRoleForm.vue | AddBuiltinRoleForm | MfwAddRoleForm | ✅ 已更新 |

**执行状态**: ✅ 全部成功

#### 2.5 更新引用
- [x] **2.5.1** 全局搜索旧组件名：`import.*from.*old-name`
- [x] **2.5.2** 替换为新组件名：`import.*from.*MfwNewName`
- [x] **2.5.3** 保存所有修改的文件

**引用更新记录**：
| 文件 | 修改内容 | 状态 |
|------|---------|------|
| `business/permission-manager/Index.vue` | `import PermissionNodeForm from './MfwPermissionNodeForm.vue'` | ✅ |
| `business/permission-manager/Index.vue` | `import PermissionValueForm from './MfwPermissionValueForm.vue'` | ✅ |
| `picker/permission-pool-panel/Index.vue` | `import PermissionValuePoolPopup from './MfwPermissionValuePoolPopup.vue'` | ✅ |
| `business/builtin-role-dialog/Index.vue` | `import AddRoleForm from './MfwAddRoleForm.vue'` | ✅ |

**执行状态**: ✅ 全部成功

#### 2.6 编写检查报告
- [x] **2.6.1** 创建文件 `docs/04-项目实施/05-任务追踪/output/命名规范检查报告.md`
- [x] **2.6.2** 写入表头：| 原名称 | 新名称 | 状态 |
- [x] **2.6.3** 逐行写入重命名记录
- [x] **2.6.4** 写入统计：共扫描 X 个文件，重命名 Y 个

**完成标志**: 所有组件文件符合 `Mfw{ComponentName}.vue` 格式

**报告文件**: [命名规范检查报告.md](../../output/命名规范检查报告.md)

---

### 3. 类型定义修复 (1h)

#### 3.1 读取类型定义文件
- [ ] **3.1.1** 打开 `packages/base-frontend/src/types/index.ts`
- [ ] **3.1.2** 打开 `packages/base-frontend/src/types/common.ts`
- [ ] **3.1.3** 打开 `packages/base-frontend/src/types/component.ts`
- [ ] **3.1.4** 如文件不存在，创建它

#### 3.2 检查基础类型定义
- [ ] **3.2.1** 搜索 `interface ComponentProps`
- [ ] **3.2.2** 检查是否有 `extends` 关键字
- [ ] **3.2.3** 检查是否包含 `id?: string | number`
- [ ] **3.2.4** 检查是否包含 `class?: string`
- [ ] **3.2.5** 如缺少，添加到接口定义中

#### 3.3 检查 Element Plus 类型兼容性
- [ ] **3.3.1** 打开 `packages/base-frontend/package.json`
- [ ] **3.3.2** 查找 `element-plus` 版本号
- [ ] **3.3.3** 运行 `pnpm list element-plus` 确认安装
- [ ] **3.3.4** 创建测试文件 `packages/base-frontend/src/types/element-plus.test-d.ts`
- [ ] **3.3.5** 写入内容：
```typescript
import type { FormProps, TableProps } from 'element-plus'

// 测试类型是否可以正常引用
type TestForm = FormProps
type TestTable = TableProps
```
- [ ] **3.3.6** 运行 `pnpm typecheck` 检查测试文件
- [ ] **3.3.7** 如有错误，在 `types/index.ts` 中添加 `import type { FormProps, TableProps } from 'element-plus'`

#### 3.4 修复类型冲突
- [ ] **3.4.1** 运行 `pnpm --filter @moyan/base-frontend typecheck`
- [ ] **3.4.2** 复制所有错误信息到 `type-errors.txt`
- [ ] **3.4.3** 对每个 "Duplicate identifier" 错误：
  - [ ] **3.4.3.1** 定位重复定义的文件和行号
  - [ ] **3.4.3.2** 保留一处定义，其他改为 `import type { X } from './xxx'`
- [ ] **3.4.4** 对每个 "Cannot find name" 错误：
  - [ ] **3.4.4.1** 添加缺失的类型定义
  - [ ] **3.4.4.2** 或导入：`import type { X } from 'xxx'`
- [ ] **3.4.5** 重新运行 typecheck 直到无错误

#### 3.5 添加类型导出
- [ ] **3.5.1** 打开 `packages/base-frontend/src/types/index.ts`
- [ ] **3.5.2** 添加 `export * from './common'`
- [ ] **3.5.3** 添加 `export * from './component'`
- [ ] **3.5.4** 添加 `export type { FormProps, TableProps } from 'element-plus'`
- [ ] **3.5.5** 保存文件

**完成标志**: `pnpm typecheck` 无错误，types/index.ts 可导出所有公共类型

---

### 4. 自测试验证 (1h)

#### 4.1 类型检查
- [ ] **4.1.1** 在 `packages/base-frontend/` 目录运行 `pnpm typecheck`
- [ ] **4.1.2** 复制输出到 `output/typecheck-result.txt`
- [ ] **4.1.3** 检查输出中是否有 `error` 关键字
- [ ] **4.1.4** 如有错误，记录到 `output/typecheck-errors.md` 并返回 3.4 修复

#### 4.2 单元测试
- [ ] **4.2.1** 在 `packages/base-frontend/` 目录运行 `pnpm vitest run`
- [ ] **4.2.2** 复制输出到 `output/vitest-result.txt`
- [ ] **4.2.3** 检查输出中的 "Pass" 和 "Fail" 数量
- [ ] **4.2.4** 如有失败，记录到 `output/vitest-failures.md`

#### 4.3 示例应用启动
- [ ] **4.3.1** 在根目录运行 `pnpm dev:examples`
- [ ] **4.3.2** 等待控制台输出 `Local: http://localhost:xxxx`
- [ ] **4.3.3** 打开浏览器访问该地址
- [ ] **4.3.4** 检查页面是否正常加载（无白屏）
- [ ] **4.3.5** 打开浏览器开发者工具 → Console
- [ ] **4.3.6** 检查是否有红色错误信息
- [ ] **4.3.7** 截图保存控制台状态到 `output/app-screenshot.png`
- [ ] **4.3.8** 按 `Ctrl+C` 停止服务

#### 4.4 编写自测试报告
- [ ] **4.4.1** 创建文件 `output/自测试报告 -20260412.md`
- [ ] **4.4.2** 写入模板内容：
```markdown
# 自测试报告

## 类型检查
- 命令：pnpm typecheck
- 结果：✅ 无错误 / ❌ X 个错误
- 详情：[typecheck-result.txt](./typecheck-result.txt)

## 单元测试
- 命令：pnpm vitest run
- 结果：X/Y 通过
- 详情：[vitest-result.txt](./vitest-result.txt)

## 应用启动
- 命令：pnpm dev:examples
- 结果：✅ 正常加载 / ❌ 加载失败
- 截图：[app-screenshot.png](./app-screenshot.png)

## 结论
[总结本次测试结果]
```
- [ ] **4.4.3** 填写实际数据
- [ ] **4.4.4** 保存文件

**完成标志**: 自测试报告完成，所有核心功能验证通过

---

## 验收标准（可量化）

| 序号 | 验收项 | 验证方法 | 通过标准 |
|------|--------|----------|----------|
| 1 | 组件导出 | 检查 index.ts | 所有导出路径正确 |
| 2 | 组件命名 | 扫描组件文件 | 框架组件 100% 使用 Mfw 前缀 |
| 3 | 类型定义 | 运行 typecheck | 无类型错误 |
| 4 | 单元测试 | 运行 vitest run | 通过率 ≥ 90% |
| 5 | 应用启动 | 手动验证 | 页面无白屏，Console 无红色错误 |

---

## 交付物清单

| 序号 | 交付物 | 文件路径 | 验收项 |
|------|--------|----------|--------|
| 1 | 组件导出修复 | `packages/base-frontend/src/index.ts` | 1 |
| 2 | 命名规范检查报告 | `docs/04-项目实施/05-任务追踪/output/命名规范检查报告.md` | 2 |
| 3 | 类型定义文件 | `packages/base-frontend/src/types/*.ts` | 3 |
| 4 | 自测试报告 | `docs/04-项目实施/05-任务追踪/output/自测试报告 -20260412.md` | 4,5 |

---

## 相关文件

- [组件命名规范](../../02-团队/01-团队规范/组件命名规范.md)
- [类型定义规范](../../04-项目实施/044-前端开发/types/README.md)

---

**状态**: 进行中 - 等待用户确认业务组件命名规范
