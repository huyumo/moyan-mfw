---
task: 系统初始化方案 - 阶段 1 种子数据修复
status: completed
priority: P0
started: 2026-04-07
completed: 2026-04-07 21:30
assignee: @ai
---

## 任务概述

执行系统初始化方案阶段 1，修复种子数据逻辑问题，确保角色绑定 appTypeId、权限池配置、拥有者绑定完整。

## 问题列表

| 序号 | 问题 | 严重性 | 状态 |
|------|------|--------|------|
| 1 | 角色 appTypeId 为 NULL | P0 | ✅ 已修复 |
| 2 | 权限池未初始化 | P0 | ✅ 已修复 |
| 3 | 拥有者绑定不完整 | P0 | ✅ 已修复 |
| 4 | 种子数据执行顺序不合理 | P1 | ✅ 已修复 |
| 5 | 重复函数定义导致编译错误 | P0 | ✅ 已修复 |

## 修复详情

### 1. 角色绑定 appTypeId 缺失

**问题**：种子数据创建的角色 `appTypeId` 字段为 NULL，不符合内置角色必须绑定应用类型的业务规则。

**根本原因**：旧种子数据代码使用 `appTypeId: undefined`，导致数据库字段为 NULL。

**修复位置**：`packages/base-backend/src/database/seeds/index.ts:382-454`

**修复代码**：
```typescript
async function seedRoles(dataSource: DataSource): Promise<void> {
  // 获取 system 应用类型 ID
  const systemAppType = await dataSource.manager.findOne(AppType, {
    where: { typeCode: 'system' },
  });

  const roles = [
    {
      id: 'a2b83a1e-b1b9-4a19-b587-2f110ee56ae9',
      roleName: '超级管理员',
      roleCode: 'super_admin',
      appTypeId: systemAppType.id, // 绑定应用类型 ID
      isBuiltin: 1,
      isOwner: 1,
      // ...
    },
    // ...
  ];

  for (const role of roles) {
    const exists = await dataSource.manager.findOne(Role, { where: { roleCode: role.roleCode } });
    if (!exists) {
      await dataSource.manager.save(Role, { ...role });
    } else {
      // 更新现有角色的 appTypeId（修复旧种子数据的绑定问题）
      await dataSource.manager.update(Role, exists.id, {
        appTypeId: role.appTypeId,
        isBuiltin: role.isBuiltin,
        isOwner: role.isOwner,
        roleDesc: role.roleDesc,
      });
    }
  }
}
```

**验证结果**：
```sql
mysql> SELECT id, roleName, roleCode, appTypeId, isBuiltin, isOwner FROM sys_roles;
+--------------------------------------+--------------+---------------+--------------------------------------+------+---------+
| id                                   | roleName     | roleCode      | appTypeId                          | isBuiltin | isOwner |
+--------------------------------------+--------------+---------------+--------------------------------------+------+---------+
| a2b83a1e-b1b9-4a19-b587-2f110ee56ae9 | 超级管理员   | super_admin   | f7eecf8d-0576-4935-bbc9-ca22b9b4bb9f |    1 |       1 |
| c3d4e5f6-a7b8-4c5d-8e9f-0a1b2c3d4e5f | 管理员       | admin         | f7eecf8d-0576-4935-bbc9-ca22b9b4bb9f |    1 |       0 |
| d4e5f6a7-b8c9-4d5e-9f0a-1b2c3d4e5f6a | 普通用户     | user          | f7eecf8d-0576-4935-bbc9-ca22b9b4bb9f |    1 |       0 |
+--------------------------------------+--------------+---------------+--------------------------------------+------+---------+
```

---

### 2. 权限池未初始化

**问题**：没有创建 `sys_app_type_permission` 记录，角色权限分配缺少权限池约束。

**修复位置**：`packages/base-backend/src/database/seeds/index.ts:753-795`

**修复代码**：
```typescript
async function seedPermissionPool(dataSource: DataSource): Promise<void> {
  process.stdout.write('  🔒 配置权限池...');

  const systemAppType = await dataSource.manager.findOne(AppType, {
    where: { typeCode: 'system' },
  });

  const allPermissions = await dataSource.manager.find(Permission, {
    where: { permissionType: PermissionType.PC },
  });

  // 为每个权限创建权限池记录
  for (const perm of allPermissions) {
    const exists = await dataSource.manager.findOne(AppTypePermissionEntity, {
      where: {
        appTypeId: systemAppType.id,
        permissionId: perm.id,
      },
    });

    if (!exists) {
      await dataSource.manager.save(AppTypePermissionEntity, {
        appTypeId: systemAppType.id,
        permissionId: perm.id,
        permissionValue: perm.permissionValue || 0n,
      });
    }
  }

  process.stdout.write(`    ✓ 配置 ${allPermissions.length} 个权限到权限池\n`);
}
```

**验证结果**：
```sql
mysql> SELECT COUNT(*) as count FROM sys_app_type_permissions WHERE appTypeId = 'f7eecf8d-0576-4935-bbc9-ca22b9b4bb9f';
+-------+
| count |
+-------+
|    21 |
+-------+
```

---

### 3. 拥有者绑定不完整

**问题**：没有在 `sys_user_app` 表创建拥有者关联记录。

**修复位置**：`packages/base-backend/src/database/seeds/index.ts:800-863`

**修复代码**：
```typescript
async function seedAppMembers(dataSource: DataSource): Promise<void> {
  process.stdout.write('  🔗 绑定拥有者...');

  const adminUser = await dataSource.manager.findOne(User, {
    where: { username: 'admin' },
  });

  const systemApp = await dataSource.manager.findOne(App, {
    where: { appCode: 'system-instance' },
  });

  const superAdminRole = await dataSource.manager.findOne(Role, {
    where: { roleCode: 'super_admin' },
  });

  // 1. 更新应用的 ownerId（如果未设置）
  if (!systemApp.ownerId) {
    systemApp.ownerId = adminUser.id;
    await dataSource.manager.save(App, systemApp);
  }

  // 2. 创建 sys_app_members 记录（拥有者）
  const memberExists = await dataSource.manager.findOne(AppMember, {
    where: {
      appId: systemApp.id,
      userId: adminUser.id,
    },
  });

  if (!memberExists) {
    await dataSource.manager.save(AppMember, {
      appId: systemApp.id,
      userId: adminUser.id,
      roleId: superAdminRole.id,
    });
  }

  // 3. 创建 sys_user_role 记录（如果不存在）
  const userRoleExists = await dataSource.manager.findOne(UserRole, {
    where: {
      userId: adminUser.id,
      roleId: superAdminRole.id,
    },
  });

  if (!userRoleExists) {
    await dataSource.manager.save(UserRole, {
      userId: adminUser.id,
      roleId: superAdminRole.id,
      createdAt: new Date(),
    });
  }

  process.stdout.write(`    ✓ 绑定 admin 用户为 ${systemApp.appName} 的拥有者\n`);
}
```

**验证结果**：
```sql
mysql> SELECT * FROM sys_app_members WHERE userId = (SELECT id FROM sys_users WHERE username = 'admin');
+----------------------------+----------------------------+------------+--------------------------------------+--------------------------------------+--------------------------------------+-----------+
| created_at                 | updated_at                 | deleted_at | id                                 | appId                                | userId                               | roleId    |
+----------------------------+----------------------------+------------+--------------------------------------+--------------------------------------+--------------------------------------+-----------+
| 2026-04-07 21:21:05.101769 | 2026-04-07 21:21:05.101769 | NULL       | a5284b99-55aa-42f5-bf40-3392b639aa7f | e38ef2a3-e5b4-4b9f-a4f3-1b5e15dc8814 | 38deb03d-615b-4ade-93d0-e173d1af40d1 | a2b83a1e-b1b9-4a19-b587-2f110ee56ae9 |
+----------------------------+----------------------------+------------+--------------------------------------+--------------------------------------+--------------------------------------+-----------+
```

---

### 4. 种子数据执行顺序调整

**问题**：原执行顺序导致依赖关系混乱。

**修复位置**：`packages/base-backend/src/database/seeds/index.ts:22-52`

**修复代码**：
```typescript
export async function runSeeds(dataSource: DataSource): Promise<void> {
  process.stdout.write('🌱 开始执行种子数据...\n');

  // 1. 初始化应用类型
  await seedAppTypes(dataSource);

  // 2. 初始化管理员账号（先创建用户，为后续依赖做准备）
  await seedAdminUser(dataSource);

  // 3. 初始化权限（基础 PC 权限）
  await seedPermissions(dataSource);

  // 4. 初始化角色（内置角色绑定 appTypeId）
  await seedRoles(dataSource);

  // 5. 初始化应用实例（依赖 admin 用户和已创建的角色）
  await seedAppInstances(dataSource);

  // 6. 配置权限池
  await seedPermissionPool(dataSource);

  // 7. 分配角色权限
  await seedRolePermissions(dataSource);

  // 8. 绑定拥有者（sys_user_app + sys_user_role）
  await seedAppMembers(dataSource);

  process.stdout.write('\n✅ 种子数据执行完成！\n');
}
```

**执行顺序**：
```
AppType → User → Permission → Role → App → Pool → RolePerm → Member
```

---

### 5. 重复函数定义修复

**问题**：`seedUserRoles` 函数重复定义（lines 592 and 666），且第一个版本引用未定义的 `allPermissions` 和 `adminRole` 变量，导致 TypeScript 编译错误。

**修复位置**：`packages/base-backend/src/database/seeds/index.ts`

**修复内容**：
- 移除第一个 broken 版本（lines 592-661）
- 保留第二个正确版本（lines 666-708）

**验证结果**：
```bash
pnpm typecheck → ✅ 无错误
```

---

## 验证结果

### 种子数据执行

```bash
# 执行种子数据
pnpm ts-node src/database/run-seeds.ts

# 输出
🌱 开始执行种子数据...
  📦 初始化应用类型...    √ 应用类型已存在：系统管理 (typeCode: system)
  👤 初始化管理员账号...    √ 用户已存在：admin
  🔐 初始化权限...    √ PC 权限根节点已存在
  👥 初始化角色...    ✓ 更新角色：超级管理员 (appTypeId: f7eecf8d-...)
  📱 初始化应用实例...    √ 应用实例已存在：系统管理后台
  🔒 配置权限池...    ✓ 配置 21 个权限到权限池
  🔗 绑定角色权限...    ✓ 超级管理员绑定 23 个权限
  🔗 绑定拥有者...    √ 拥有者绑定已存在：admin → 系统管理后台
✅ 种子数据执行完成！
```

### API 验证

```bash
# 登录
POST /api/auth/login
{"username":"admin","password":"Admin@123"}
→ {"code":0,"data":{"accessToken":"..."}}

# 应用列表
GET /api/auth/apps
→ {"code":0,"data":[{"appId":"e38ef2a3-e5b4-4b9f-a4f3-1b5e15dc8814","appName":"系统管理后台","appCode":"system-instance","appTypeId":"f7eecf8d-0576-4935-bbc9-ca22b9b4bb9f","role":"owner"}]}

# 权限菜单
GET /api/auth/permissions?appId=e38ef2a3-e5b4-4b9f-a4f3-1b5e15dc8814
→ {"code":0,"data":{"menuTree":[...],"permissions":[23 个权限编码]}}
```

---

## 修改文件清单

1. `packages/base-backend/src/database/seeds/index.ts` - 主要修改文件
   - 修复 `seedRoles()` 函数绑定 `appTypeId`
   - 添加 `seedPermissionPool()` 函数
   - 添加 `seedAppMembers()` 函数
   - 调整 `runSeeds()` 执行顺序
   - 移除重复的 `seedUserRoles()` 函数
   - 添加角色更新逻辑（修复旧数据）

---

## 关键决策

- **角色更新策略**：对于已存在的角色，使用 `update()` 方法更新 `appTypeId` 字段，确保旧数据也能被修复
- **权限池范围**：只配置 PC 权限到权限池（`permissionType: PermissionType.PC`），因为 system 应用类型只使用 PC 权限
- **拥有者绑定**：同时创建 `sys_app_members` 和 `sys_user_role` 记录，确保应用绑定和角色绑定都完整

---

## 交付物

- ✅ 种子数据逻辑修复完成
- ✅ 数据库验证通过
- ✅ API 验证通过
- ✅ 类型检查通过
- ✅ 任务文档归档完成

## 备注

- 应用类型 ID: `f7eecf8d-0576-4935-bbc9-ca22b9b4bb9f`
- system-instance 应用 ID: `e38ef2a3-e5b4-4b9f-a4f3-1b5e15dc8814`
- admin 用户 ID: `38deb03d-615b-4ade-93d0-e173d1af40d1`
- 超级管理员角色 ID: `a2b83a1e-b1b9-4a19-b587-2f110ee56ae9`
- 权限池记录数：21 条（所有 PC 权限）

## 下一步

阶段 2：初始化检测机制（InstallService + InstallController）
- 创建 `InstallService` 服务
- 实现 `isInitialized()` 方法
- 创建 `InstallController` 提供 API 接口
- 在 `main.ts` 中启动时检测初始化
