---
title: 2026-04-28-005-P1-核心组件测试补充
status: draft
version: 2.0.0
created: 2026-04-12
updated: 2026-04-12T20:00:00Z

task_id: "2026-04-28-005"
task_status: pending
task_priority: P1
task_assignee: QA-Agent
task_reviewers: [Frontend-Dev]
task_session: session-2026-04-12-009
task_hours: 3h
deadline: 2026-04-28

review_cycle: on-task-complete
---

## 任务目标

为核心组件补充测试用例，提升覆盖率至 90% 以上。

---

## WBS 工作分解

```
核心组件测试补充 (3h)
├── 1. 测试覆盖率分析 (20min)
├── 2. FormCard 测试补充 (50min)
├── 3. TableList 测试补充 (50min)
├── 4. Upload 测试补充 (40min)
└── 5. 测试报告编写 (20min)
```

---

## TODO List（原子级细化）

### 1. 测试覆盖率分析 (20min)

#### 1.1 运行覆盖率报告
- [ ] **1.1.1** 在 `packages/base-frontend/` 目录运行 `pnpm vitest run --coverage`
- [ ] **1.1.2** 等待命令执行完成
- [ ] **1.1.3** 打开覆盖率报告 `packages/base-frontend/coverage/index.html`
- [ ] **1.1.4** 在浏览器中查看报告

#### 1.2 记录低覆盖率组件
- [ ] **1.2.1** 找到 `MfwFormCard` 组件覆盖率
- [ ] **1.2.2** 记录当前覆盖率到笔记
- [ ] **1.2.3** 找到 `MfwTableList` 组件覆盖率
- [ ] **1.2.4** 记录当前覆盖率到笔记
- [ ] **1.2.5** 找到 `MfwUpload` 组件覆盖率
- [ ] **1.2.6** 记录当前覆盖率到笔记

#### 1.3 创建测试计划
- [ ] **1.3.1** 创建文件 `output/测试补充计划.md`
- [ ] **1.3.2** 写入目标：
```markdown
# 测试补充计划

## 目标覆盖率
- FormCard: 当前 X% → 目标 90%
- TableList: 当前 X% → 目标 90%
- Upload: 当前 X% → 目标 90%

## 待补充测试
- FormCard: 表单验证、提交、重置
- TableList: 分页、排序、筛选
- Upload: 文件选择、上传、删除
```
- [ ] **1.3.3** 保存文件

**完成标志**: 覆盖率基准确认，测试计划明确

---

### 2. FormCard 测试补充 (50min)

#### 2.1 读取现有测试
- [ ] **2.1.1** 打开 `src/components/MfwFormCard/__tests__/MfwFormCard.test.ts`
- [ ] **2.1.2** 阅读现有测试用例
- [ ] **2.1.3** 记录缺失的测试场景

#### 2.2 补充表单验证测试
- [ ] **2.2.1** 在测试文件中添加 `describe('表单验证', () => {...})`
- [ ] **2.2.2** 编写测试用例：验证必填字段
```typescript
it('验证必填字段', async () => {
  const wrapper = mount(MfwFormCard, {
    props: {
      model: { name: '' },
      rules: { name: [{ required: true, message: '姓名必填' }] }
    }
  })
  await wrapper.find('button[type="submit"]').trigger('click')
  expect(wrapper.text()).toContain('姓名必填')
})
```
- [ ] **2.2.3** 保存文件

#### 2.3 补充表单提交测试
- [ ] **2.3.1** 编写测试用例：提交成功
```typescript
it('提交成功事件', async () => {
  const wrapper = mount(MfwFormCard, {
    props: {
      model: { name: 'test' },
      rules: {}
    }
  })
  await wrapper.find('button[type="submit"]').trigger('click')
  expect(wrapper.emitted('submit')).toBeTruthy()
  expect(wrapper.emitted('submit')?.[0]).toEqual([{ name: 'test' }])
})
```
- [ ] **2.3.2** 保存文件

#### 2.4 补充表单重置测试
- [ ] **2.4.1** 编写测试用例：调用 resetFields 方法
```typescript
it('重置表单', async () => {
  const wrapper = mount(MfwFormCard, {
    props: {
      model: { name: 'test' }
    }
  })
  await wrapper.vm.resetFields()
  expect(wrapper.props('model').name).toBe('')
})
```
- [ ] **2.4.2** 保存文件

#### 2.5 运行测试
- [ ] **2.5.1** 运行 `pnpm vitest run MfwFormCard`
- [ ] **2.5.2** 检查所有测试通过
- [ ] **2.5.3** 如失败，修复后重试

**完成标志**: FormCard 测试覆盖率 ≥ 90%

---

### 3. TableList 测试补充 (50min)

#### 3.1 读取现有测试
- [ ] **3.1.1** 打开 `src/components/MfwTableList/__tests__/MfwTableList.test.ts`
- [ ] **3.1.2** 阅读现有测试用例
- [ ] **3.1.3** 记录缺失的测试场景

#### 3.2 补充分页测试
- [ ] **3.2.1** 编写测试用例：页码切换
```typescript
it('页码切换事件', async () => {
  const wrapper = mount(MfwTableList, {
    props: {
      data: mockData,
      total: 100,
      currentPage: 1
    }
  })
  await wrapper.find('.el-pager li:nth-child(2)').trigger('click')
  expect(wrapper.emitted('page-change')).toBeTruthy()
  expect(wrapper.emitted('page-change')?.[0]).toEqual([2])
})
```
- [ ] **3.2.2** 保存文件

#### 3.3 补充排序测试
- [ ] **3.3.1** 编写测试用例：列排序
```typescript
it('列排序事件', async () => {
  const wrapper = mount(MfwTableList, {
    props: {
      data: mockData,
      columns: [{ prop: 'name', label: '姓名', sortable: true }]
    }
  })
  await wrapper.find('.el-table__header th .caret-wrapper').trigger('click')
  expect(wrapper.emitted('sort-change')).toBeTruthy()
})
```
- [ ] **3.3.2** 保存文件

#### 3.4 补充行操作测试
- [ ] **3.4.1** 编写测试用例：行点击
```typescript
it('行点击事件', async () => {
  const wrapper = mount(MfwTableList, {
    props: { data: mockData }
  })
  await wrapper.find('.el-table__row').trigger('click')
  expect(wrapper.emitted('row-click')).toBeTruthy()
})
```
- [ ] **3.4.2** 保存文件

#### 3.5 补充选择测试
- [ ] **3.5.1** 编写测试用例：多选
```typescript
it('多选事件', async () => {
  const wrapper = mount(MfwTableList, {
    props: { data: mockData }
  })
  await wrapper.find('.el-checkbox').trigger('click')
  expect(wrapper.emitted('selection-change')).toBeTruthy()
})
```
- [ ] **3.5.2** 保存文件

#### 3.6 运行测试
- [ ] **3.6.1** 运行 `pnpm vitest run MfwTableList`
- [ ] **3.6.2** 检查所有测试通过
- [ ] **3.6.3** 如失败，修复后重试

**完成标志**: TableList 测试覆盖率 ≥ 90%

---

### 4. Upload 测试补充 (40min)

#### 4.1 读取现有测试
- [ ] **4.1.1** 打开 `src/components/MfwUpload/__tests__/MfwUpload.test.ts`
- [ ] **4.1.2** 阅读现有测试用例
- [ ] **4.1.3** 记录缺失的测试场景

#### 4.2 补充文件选择测试
- [ ] **4.2.1** 编写测试用例：选择文件
```typescript
it('选择文件事件', async () => {
  const wrapper = mount(MfwUpload, {
    props: { action: '/api/upload' }
  })
  const file = new File(['test'], 'test.txt', { type: 'text/plain' })
  await wrapper.vm.handleChange(file, [file])
  expect(wrapper.emitted('change')).toBeTruthy()
})
```
- [ ] **4.2.2** 保存文件

#### 4.3 补充上传成功测试
- [ ] **4.3.1** 编写测试用例：上传成功回调
```typescript
it('上传成功事件', async () => {
  const wrapper = mount(MfwUpload, {
    props: { action: '/api/upload' }
  })
  const mockResponse = { code: 200, url: 'http://xxx/file.txt' }
  await wrapper.vm.handleSuccess(mockResponse, { name: 'test.txt' })
  expect(wrapper.emitted('success')).toBeTruthy()
  expect(wrapper.emitted('success')?.[0]).toEqual([mockResponse])
})
```
- [ ] **4.3.2** 保存文件

#### 4.4 补充上传失败测试
- [ ] **4.4.1** 编写测试用例：上传失败回调
```typescript
it('上传失败事件', async () => {
  const wrapper = mount(MfwUpload, {
    props: { action: '/api/upload' }
  })
  const mockError = new Error('Upload failed')
  await wrapper.vm.handleError(mockError, { name: 'test.txt' })
  expect(wrapper.emitted('error')).toBeTruthy()
})
```
- [ ] **4.4.2** 保存文件

#### 4.5 补充删除测试
- [ ] **4.5.1** 编写测试用例：删除文件
```typescript
it('删除文件事件', async () => {
  const wrapper = mount(MfwUpload, {
    props: { action: '/api/upload' }
  })
  const file = { name: 'test.txt', uid: 1 }
  await wrapper.vm.handleRemove(file)
  expect(wrapper.emitted('remove')).toBeTruthy()
})
```
- [ ] **4.5.2** 保存文件

#### 4.6 补充进度测试
- [ ] **4.6.1** 编写测试用例：上传进度
```typescript
it('上传进度事件', async () => {
  const wrapper = mount(MfwUpload, {
    props: { action: '/api/upload' }
  })
  const mockEvent = new ProgressEvent('progress', { loaded: 50, total: 100 })
  await wrapper.vm.handleProgress(mockEvent)
  expect(wrapper.emitted('progress')).toBeTruthy()
})
```
- [ ] **4.6.2** 保存文件

#### 4.7 运行测试
- [ ] **4.7.1** 运行 `pnpm vitest run MfwUpload`
- [ ] **4.7.2** 检查所有测试通过
- [ ] **4.7.3** 如失败，修复后重试

**完成标志**: Upload 测试覆盖率 ≥ 90%

---

### 5. 测试报告编写 (20min)

#### 5.1 运行覆盖率报告
- [ ] **5.1.1** 运行 `pnpm vitest run --coverage`
- [ ] **5.1.2** 打开覆盖率报告
- [ ] **5.1.3** 记录三个组件的最终覆盖率

#### 5.2 编写测试报告
- [ ] **5.2.1** 创建文件 `output/核心组件测试补充报告 -20260428.md`
- [ ] **5.2.2** 写入内容：
```markdown
# 核心组件测试补充报告

## 测试覆盖率

| 组件 | 补充前 | 补充后 | 目标 | 达成 |
|------|--------|--------|------|------|
| MfwFormCard | X% | X% | 90% | ✅/❌ |
| MfwTableList | X% | X% | 90% | ✅/❌ |
| MfwUpload | X% | X% | 90% | ✅/❌ |

## 新增测试用例

### MfwFormCard
- [ ] 验证必填字段
- [ ] 提交成功事件
- [ ] 重置表单

### MfwTableList
- [ ] 页码切换
- [ ] 列排序
- [ ] 行点击
- [ ] 多选

### MfwUpload
- [ ] 选择文件
- [ ] 上传成功
- [ ] 上传失败
- [ ] 删除文件
- [ ] 上传进度

## 结论
✅/❌ 所有组件测试覆盖率达成 90% 目标
```
- [ ] **5.2.3** 填写实际数据
- [ ] **5.2.4** 保存文件

**完成标志**: 测试报告完成，数据准确

---

## 验收标准（可量化）

| 序号 | 验收项 | 验证方法 | 通过标准 |
|------|--------|----------|----------|
| 1 | FormCard 覆盖率 | 运行 coverage | ≥ 90% |
| 2 | TableList 覆盖率 | 运行 coverage | ≥ 90% |
| 3 | Upload 覆盖率 | 运行 coverage | ≥ 90% |
| 4 | 测试通过数 | 运行 vitest run | 失败数 = 0 |
| 5 | 测试报告 | 检查文档 | 报告完整，数据准确 |

---

## 交付物清单

| 序号 | 交付物 | 文件路径 | 验收项 |
|------|--------|----------|--------|
| 1 | 测试补充计划 | `output/测试补充计划.md` | 1 |
| 2 | FormCard 测试 | `src/components/MfwFormCard/__tests__/*.test.ts` | 1 |
| 3 | TableList 测试 | `src/components/MfwTableList/__tests__/*.test.ts` | 2 |
| 4 | Upload 测试 | `src/components/MfwUpload/__tests__/*.test.ts` | 3 |
| 5 | 测试报告 | `output/核心组件测试补充报告 -20260428.md` | 5 |

---

## 相关文件

- [Vitest 文档](https://vitest.dev/)
- [Vue Test Utils 文档](https://test-utils.vuejs.org/)

---

**状态**: 待开始
