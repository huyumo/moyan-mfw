---
title: 2026-04-30-003-P2-组件开发 -MfwSkeleton
status: draft
version: 2.0.0
created: 2026-04-12
updated: 2026-04-12T19:45:00Z

task_id: "2026-04-30-003"
task_status: pending
task_priority: P2
task_assignee: Frontend-Dev
task_reviewers: [Tech-Architect]
task_session: session-2026-04-12-009
task_hours: 1h
deadline: 2026-04-30

review_cycle: on-task-complete
---

## 任务目标

开发 `MfwSkeleton` 骨架屏组件，支持加载状态、动画、自定义行数。

---

## WBS 工作分解

```
MfwSkeleton 组件开发 (1h)
├── 1. 需求分析与设计 (10min)
├── 2. 组件结构开发 (25min)
├── 3. 样式与主题开发 (5min)
└── 4. 测试与文档 (20min)
```

---

## TODO List（原子级细化）

### 1. 需求分析与设计 (10min)

#### 1.1 参考现有实现
- [ ] **1.1.1** 打开 Element Plus Skeleton 文档 `https://element-plus.org/zh-CN/component/skeleton.html`
- [ ] **1.1.2** 记录 Props（至少 4 个：loading、rows、animated、throttle 等）
- [ ] **1.1.3** 记录插槽（default、template）

#### 1.2 定义组件 API
- [ ] **1.2.1** 创建文件 `packages/base-frontend/src/components/MfwSkeleton/MfwSkeleton.types.ts`
- [ ] **1.2.2** 写入 Props 接口：
```typescript
export interface MfwSkeletonProps {
  loading?: boolean
  rows?: number
  animated?: boolean
  throttle?: number
  showHeader?: boolean
  showAvatar?: boolean
}
```
- [ ] **1.2.3** 写入 Emits 接口（无）
- [ ] **1.2.4** 保存文件

**完成标志**: Props 定义清晰

---

### 2. 组件结构开发 (25min)

#### 2.1 创建组件目录
- [ ] **2.1.1** 创建目录 `packages/base-frontend/src/components/MfwSkeleton/`
- [ ] **2.1.2** 创建文件 `MfwSkeleton.vue`
- [ ] **2.1.3** 创建文件 `index.ts`

#### 2.2 编写组件模板
- [ ] **2.2.1** 在 `MfwSkeleton.vue` 写入 `<template>`:
```vue
<template>
  <el-skeleton
    :loading="loading"
    :rows="rows"
    :animated="animated"
    :throttle="throttle"
  >
    <template #template>
      <slot name="template">
        <el-skeleton-item variant="header" v-if="showHeader" />
        <el-skeleton-item variant="avatar" v-if="showAvatar" />
        <el-skeleton-item variant="paragraph" />
      </slot>
    </template>
    <slot></slot>
  </el-skeleton>
</template>
```
- [ ] **2.2.2** 保存文件

#### 2.3 编写组件脚本
- [ ] **2.3.1** 在 `MfwSkeleton.vue` 写入 `<script setup lang="ts">`:
```typescript
import { defineOptions } from 'vue'
import type { MfwSkeletonProps } from './MfwSkeleton.types'

defineOptions({ name: 'MfwSkeleton' })

const props = withDefaults(defineProps<MfwSkeletonProps>(), {
  loading: true,
  rows: 3,
  animated: true,
  throttle: 0,
  showHeader: false,
  showAvatar: false
})
```
- [ ] **2.3.2** 保存文件

#### 2.4 编写组件入口
- [ ] **2.4.1** 在 `index.ts` 写入标准导出代码
- [ ] **2.4.2** 保存文件

**完成标志**: 组件可被导入和使用

---

### 3. 样式与主题开发 (5min)

#### 3.1 添加组件样式
- [ ] **3.1.1** 在 `MfwSkeleton.vue` 添加 `<style scoped>`
- [ ] **3.1.2** 保存文件

**完成标志**: 样式符合设计规范

---

### 4. 测试与文档 (20min)

#### 4.1 编写单元测试
- [ ] **4.1.1** 创建文件 `__tests__/MfwSkeleton.test.ts`
- [ ] **4.1.2** 写入测试：
```typescript
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import MfwSkeleton from '../MfwSkeleton'

describe('MfwSkeleton', () => {
  it('渲染基础结构', () => {
    const wrapper = mount(MfwSkeleton, {
      props: { loading: true, rows: 3 }
    })
    expect(wrapper.classes()).toContain('el-skeleton')
  })

  it '显示动画', () => {
    const wrapper = mount(MfwSkeleton, {
      props: { loading: true, animated: true }
    })
    expect(wrapper.find('.el-skeleton__item').classes()).toContain('is-animated')
  })
})
```
- [ ] **4.1.3** 保存文件

#### 4.2 运行测试
- [ ] **4.2.1** 运行 `pnpm --filter @moyan/base-frontend vitest run MfwSkeleton`
- [ ] **4.2.2** 检查测试通过率

#### 4.3 编写使用文档
- [ ] **4.3.1** 创建文件 `docs/04-项目实施/04-前端开发/components/MfwSkeleton.md`
- [ ] **4.3.2** 写入基础用法、Props、Slots

**完成标志**: 测试通过，文档完整

---

## 验收标准

| 序号 | 验收项 | 验证方法 | 通过标准 |
|------|--------|----------|----------|
| 1 | Props 定义 | 检查 types 文件 | 至少 5 个 Props |
| 2 | 组件结构 | 运行 typecheck | 无类型错误 |
| 3 | 加载状态 | 切换 loading | 显示/隐藏骨架屏 |
| 4 | 动画效果 | animated=true | 显示动画类名 |
| 5 | 单元测试 | `pnpm vitest run` | 2/2 测试通过 |

---

## 交付物清单

| 序号 | 交付物 | 文件路径 |
|------|--------|----------|
| 1 | 类型定义 | `src/components/MfwSkeleton/MfwSkeleton.types.ts` |
| 2 | 组件实现 | `src/components/MfwSkeleton/MfwSkeleton.vue` |
| 3 | 组件入口 | `src/components/MfwSkeleton/index.ts` |
| 4 | 测试文件 | `src/components/MfwSkeleton/__tests__/MfwSkeleton.test.ts` |
| 5 | 使用文档 | `docs/04-项目实施/04-前端开发/components/MfwSkeleton.md` |

---

**状态**: 待开始
