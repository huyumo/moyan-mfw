---
title: 2026-04-30-004-P2-组件开发 -MfwEmpty
status: draft
version: 2.0.0
created: 2026-04-12
updated: 2026-04-12T19:45:00Z

task_id: "2026-04-30-004"
task_status: pending
task_priority: P2
task_assignee: Frontend-Dev
task_reviewers: [Tech-Architect]
task_session: session-2026-04-12-009
task_hours: 1h
deadline: 2026-04-30

review_cycle: on-task-complete
---

## 任务目标

开发 `MfwEmpty` 空状态组件，支持自定义图片、描述、操作按钮。

---

## WBS 工作分解

```
MfwEmpty 组件开发 (1h)
├── 1. 需求分析与设计 (10min)
├── 2. 组件结构开发 (25min)
├── 3. 样式与主题开发 (5min)
└── 4. 测试与文档 (20min)
```

---

## TODO List（原子级细化）

### 1. 需求分析与设计 (10min)

#### 1.1 参考现有实现
- [ ] **1.1.1** 打开 Element Plus Empty 文档 `https://element-plus.org/zh-CN/component/empty.html`
- [ ] **1.1.2** 记录 Props（至少 3 个：image、description、imageSize 等）
- [ ] **1.1.3** 记录插槽（default、image、description）

#### 1.2 定义组件 API
- [ ] **1.2.1** 创建文件 `packages/base-frontend/src/components/MfwEmpty/MfwEmpty.types.ts`
- [ ] **1.2.2** 写入 Props 接口：
```typescript
export interface MfwEmptyProps {
  image?: string
  description?: string
  imageSize?: number | string
}
```
- [ ] **1.2.3** 写入 Emits 接口（无）
- [ ] **1.2.4** 保存文件

**完成标志**: Props 定义清晰

---

### 2. 组件结构开发 (25min)

#### 2.1 创建组件目录
- [ ] **2.1.1** 创建目录 `packages/base-frontend/src/components/MfwEmpty/`
- [ ] **2.1.2** 创建文件 `MfwEmpty.vue`
- [ ] **2.1.3** 创建文件 `index.ts`

#### 2.2 编写组件模板
- [ ] **2.2.1** 在 `MfwEmpty.vue` 写入 `<template>`:
```vue
<template>
  <el-empty
    :image="image"
    :description="description"
    :image-size="imageSize"
  >
    <template #default>
      <slot>
        <el-button type="primary" v-if="$slots.actions">
          <slot name="actions"></slot>
        </el-button>
      </slot>
    </template>
    <template #image>
      <slot name="image">
        <el-icon v-if="imageIcon" :size="imageSize">
          <component :is="imageIcon" />
        </el-icon>
      </slot>
    </template>
    <template #description>
      <slot name="description">{{ description }}</slot>
    </template>
  </el-empty>
</template>
```
- [ ] **2.2.2** 保存文件

#### 2.3 编写组件脚本
- [ ] **2.3.1** 在 `MfwEmpty.vue` 写入 `<script setup lang="ts">`:
```typescript
import { defineOptions } from 'vue'
import type { MfwEmptyProps } from './MfwEmpty.types'

defineOptions({ name: 'MfwEmpty' })

const props = withDefaults(defineProps<MfwEmptyProps>(), {
  description: '暂无数据',
  imageSize: 160
})

const imageIcon = ref<string>()
```
- [ ] **2.3.2** 保存文件

#### 2.4 编写组件入口
- [ ] **2.4.1** 在 `index.ts` 写入标准导出代码
- [ ] **2.4.2** 保存文件

**完成标志**: 组件可被导入和使用

---

### 3. 样式与主题开发 (5min)

#### 3.1 添加组件样式
- [ ] **3.1.1** 在 `MfwEmpty.vue` 添加 `<style scoped>`
- [ ] **3.1.2** 保存文件

**完成标志**: 样式符合设计规范

---

### 4. 测试与文档 (20min)

#### 4.1 编写单元测试
- [ ] **4.1.1** 创建文件 `__tests__/MfwEmpty.test.ts`
- [ ] **4.1.2** 写入测试：
```typescript
import { describe, it, expect } from 'vitest'
import { mount } from '@vue/test-utils'
import MfwEmpty from '../MfwEmpty'

describe('MfwEmpty', () => {
  it('渲染基础结构', () => {
    const wrapper = mount(MfwEmpty, {
      props: { description: '暂无数据' }
    })
    expect(wrapper.classes()).toContain('el-empty')
  })

  it '显示自定义描述', () => {
    const wrapper = mount(MfwEmpty, {
      props: { description: '自定义描述' }
    })
    expect(wrapper.find('.el-empty__description').text()).toBe('自定义描述')
  })
})
```
- [ ] **4.1.3** 保存文件

#### 4.2 运行测试
- [ ] **4.2.1** 运行 `pnpm --filter @moyan/base-frontend vitest run MfwEmpty`
- [ ] **4.2.2** 检查测试通过率

#### 4.3 编写使用文档
- [ ] **4.3.1** 创建文件 `docs/04-项目实施/04-前端开发/components/MfwEmpty.md`
- [ ] **4.3.2** 写入基础用法、Props、Slots

**完成标志**: 测试通过，文档完整

---

## 验收标准

| 序号 | 验收项 | 验证方法 | 通过标准 |
|------|--------|----------|----------|
| 1 | Props 定义 | 检查 types 文件 | 至少 3 个 Props |
| 2 | 组件结构 | 运行 typecheck | 无类型错误 |
| 3 | 自定义描述 | 传入 description | 显示正确文本 |
| 4 | 自定义插槽 | 使用 image 插槽 | 显示自定义内容 |
| 5 | 单元测试 | `pnpm vitest run` | 2/2 测试通过 |

---

## 交付物清单

| 序号 | 交付物 | 文件路径 |
|------|--------|----------|
| 1 | 类型定义 | `src/components/MfwEmpty/MfwEmpty.types.ts` |
| 2 | 组件实现 | `src/components/MfwEmpty/MfwEmpty.vue` |
| 3 | 组件入口 | `src/components/MfwEmpty/index.ts` |
| 4 | 测试文件 | `src/components/MfwEmpty/__tests__/MfwEmpty.test.ts` |
| 5 | 使用文档 | `docs/04-项目实施/04-前端开发/components/MfwEmpty.md` |

---

**状态**: 待开始
