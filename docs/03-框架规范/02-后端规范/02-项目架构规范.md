# 项目架构规范

> 状态：**已完成** | 版本：2.0.0 | 最后更新：2026-03-30

---

## 2.1 分层架构

### 2.1.1 经典三层架构

```
┌─────────────────────────────────────────────────────────┐
│                    表现层 (Controller)                    │
│                    HTTP 接口、请求响应                      │
├─────────────────────────────────────────────────────────┤
│                    业务层 (Service)                       │
│                   业务逻辑、事务处理                       │
├─────────────────────────────────────────────────────────┤
│                  持久层 (DAO/Repository)                  │
│                   数据访问、CRUD 操作                        │
├─────────────────────────────────────────────────────────┤
│                    数据层 (Database)                      │
│                   MySQL/PostgreSQL 等                      │
└─────────────────────────────────────────────────────────┘
```

### 2.1.2 各层职责

| 层级 | 职责 | 依赖方向 | 示例 |
|------|------|----------|------|
| Controller 层 | 接收 HTTP 请求、参数验证、调用 Service、返回响应 | → Service | `UserController.createUser()` |
| Service 层 | 业务逻辑、事务管理、调用 DAO | → DAO | `UserService.register()` |
| DAO 层 | 数据库操作、CRUD、查询构建 | → Database | `UserDAO.findById()` |
| Database 层 | 数据存储、索引、事务支持 | - | MySQL 表 |

### 2.1.3 分层示例

```typescript
// ========== Controller 层 ==========
@Controller('users')
export class UserController {
  constructor(private userService: UserService) {}

  @Post()
  async createUser(@Body() dto: CreateUserDTO): Promise<ApiResponse<UserDTO>> {
    const user = await this.userService.createUser(dto);
    return ApiResponse.success(user);
  }
}

// ========== Service 层 ==========
@Injectable()
export class UserService {
  constructor(
    private userDAO: UserDAO,
    private roleDAO: RoleDAO,
    private eventEmitter: EventEmitter,
  ) {}

  @Transactional()
  async createUser(dto: CreateUserDTO): Promise<UserDTO> {
    // 1. 检查用户名是否存在
    const exists = await this.userDAO.findByUsername(dto.username);
    if (exists) {
      throw new UserExistsError(dto.username);
    }

    // 2. 创建用户
    const user = await this.userDAO.create({
      username: dto.username,
      password: await bcrypt.hash(dto.password, 10),
    });

    // 3. 绑定默认角色
    const defaultRole = await this.roleDAO.getDefault();
    await this.userDAO.bindRole(user.id, defaultRole.id);

    // 4. 发送事件
    this.eventEmitter.emit('user.created', user);

    return this.toDTO(user);
  }
}

// ========== DAO 层 ==========
@Injectable()
export class UserDAO {
  constructor(
    @InjectRepository(UserEntity)
    private repo: Repository<UserEntity>,
  ) {}

  async findById(id: string): Promise<UserEntity | null> {
    return this.repo.findOne({ where: { id } });
  }

  async findByUsername(username: string): Promise<UserEntity | null> {
    return this.repo.findOne({ where: { username } });
  }

  async create(data: CreateUserData): Promise<UserEntity> {
    const user = this.repo.create(data);
    return this.repo.save(user);
  }
}
```

---

## 2.2 模块划分

### 2.2.1 按业务领域划分模块

```
src/
├── modules/
│   ├── user/              # 用户模块
│   │   ├── UserController.ts
│   │   ├── UserService.ts
│   │   ├── UserDAO.ts
│   │   ├── UserEntity.ts
│   │   ├── UserDTO.ts
│   │   └── user.module.ts
│   ├── role/              # 角色模块
│   │   ├── RoleController.ts
│   │   ├── RoleService.ts
│   │   ├── RoleDAO.ts
│   │   ├── RoleEntity.ts
│   │   ├── RoleDTO.ts
│   │   └── role.module.ts
│   ├── permission/        # 权限模块
│   │   └── ...
│   ├── auth/              # 认证模块
│   │   └── ...
│   └── app/               # 应用模块
│       └── ...
└── common/                # 公共模块
    ├── middleware/
    ├── utils/
    ├── types/
    └── decorators/
```

### 2.2.2 模块依赖关系

```
┌──────────────────────────────────────────────────┐
│                  AppModule                        │
│            （根模块，导入所有业务模块）               │
└─────────────────┬────────────────────────────────┘
                  │
    ┌─────────────┼─────────────┐
    │             │             │
┌───▼───┐    ┌───▼───┐    ┌───▼───┐
│ User  │    │ Role  │    │  Auth │
│ Module│    │ Module│    │ Module│
└───┬───┘    └───┬───┘    └───┬───┘
    │            │            │
    └────────────┼────────────┘
                 │
         ┌───────▼───────┐
         │ Common Module │
         │  （工具、类型）  │
         └───────────────┘
```

### 2.2.3 模块间通信

**原则：**
- 模块间通过 Service 接口通信
- 避免循环依赖
- 使用事件解耦

```typescript
// ✅ 推荐：使用事件解耦
// user.module.ts
@Module({
  imports: [
    // 导入需要的模块
    RoleModule,
  ],
  providers: [UserService, UserDAO],
  controllers: [UserController],
  exports: [UserService],  // 导出服务供其他模块使用
})
export class UserModule {}

// ✅ 推荐：使用事件总线
// 在 Service 中发送事件
this.eventEmitter.emit('user.created', { user });

// 在另一个模块中监听
@OnEvent('user.created')
handleUserCreated(payload: { user: User }) {
  // 发送欢迎邮件
  this.emailService.sendWelcome(payload.user);
}
```

---

## 2.3 依赖注入

### 2.3.1 构造函数注入

```typescript
// ✅ 推荐
@Injectable()
export class UserService {
  constructor(
    private userDAO: UserDAO,
    private roleDAO: RoleDAO,
    private logger: Logger,
  ) {}
}

// ❌ 避免：在类内部实例化依赖
export class UserService {
  private userDAO = new UserDAO();  // 难以测试
}
```

### 2.3.2 接口注入

```typescript
// 定义接口
export interface IUserService {
  getUserById(id: string): Promise<User>;
}

// 实现类
@Injectable()
export class UserService implements IUserService {
  async getUserById(id: string): Promise<User> {
    // ...
  }
}

// 使用接口注入
@Controller('users')
export class UserController {
  constructor(
    @Inject('IUserService')
    private userService: IUserService,
  ) {}
}
```

---

## 2.4 配置管理

### 2.4.1 配置文件结构

```typescript
// config/database.config.ts
export default () => ({
  type: 'mysql',
  host: process.env.DB_HOST,
  port: parseInt(process.env.DB_PORT, 10) || 3306,
  username: process.env.DB_USERNAME,
  password: process.env.DB_PASSWORD,
  database: process.env.DB_NAME,
});

// config/app.config.ts
export default () => ({
  port: parseInt(process.env.PORT, 10) || 3000,
  env: process.env.NODE_ENV || 'development',
  cors: {
    origin: process.env.CORS_ORIGIN || '*',
  },
});
```

### 2.4.2 配置使用

```typescript
// 在 Module 中加载配置
@Module({
  imports: [
    ConfigModule.forRoot({
      load: [databaseConfig, appConfig],
    }),
    TypeOrmModule.forRootAsync({
      imports: [ConfigModule],
      useFactory: (configService: ConfigService) => ({
        type: 'mysql',
        host: configService.get('database.host'),
        // ...
      }),
      inject: [ConfigService],
    }),
  ],
})
export class AppModule {}

// 在 Service 中使用
@Injectable()
export class UserService {
  constructor(
    @Inject('DB_CONNECTION')
    private dbConnection: Connection,
  ) {}
}
```

---

## 2.5 中间件规范

### 2.5.1 中间件类型

| 中间件 | 用途 | 执行时机 |
|--------|------|----------|
| 认证中间件 | 验证用户身份 | 路由处理前 |
| 日志中间件 | 记录请求日志 | 请求开始/结束 |
| 异常处理中间件 | 统一异常处理 | 控制器异常抛出后 |
| CORS 中间件 | 跨域处理 | 响应前 |
| 限流中间件 | 请求频率限制 | 路由处理前 |

### 2.5.2 中间件实现

```typescript
// 认证中间件
@Injectable()
export class AuthMiddleware implements NestMiddleware {
  constructor(private jwtService: JwtService) {}

  use(req: Request, res: Response, next: NextFunction) {
    const token = this.extractToken(req);
    if (!token) {
      throw new UnauthorizedError();
    }

    try {
      const payload = this.jwtService.verify(token);
      req.user = payload;
      next();
    } catch (error) {
      throw new UnauthorizedError();
    }
  }
}
```

---

## 2.6 异常处理

### 2.6.1 自定义异常

```typescript
// 业务异常基类
export class BusinessException extends HttpException {
  constructor(
    message: string,
    code: number,
    status: HttpStatus,
  ) {
    super({ code, message, data: null }, status);
  }
}

// 具体业务异常
export class UserNotFoundError extends BusinessException {
  constructor(id: string) {
    super(`用户 ${id} 不存在`, 10001, HttpStatus.NOT_FOUND);
  }
}

export class UserExistsError extends BusinessException {
  constructor(username: string) {
    super(`用户名 ${username} 已存在`, 10002, HttpStatus.CONFLICT);
  }
}
```

### 2.6.2 全局异常过滤器

```typescript
@Catch()
export class AllExceptionsFilter implements ExceptionFilter {
  private readonly logger = new Logger();

  catch(exception: unknown, host: ArgumentsHost) {
    const ctx = host.switchToHttp();
    const response = ctx.getResponse<Response>();
    const request = ctx.getRequest<Request>();

    let status = HttpStatus.INTERNAL_SERVER_ERROR;
    let code = 50000;
    let message = '服务器内部错误';

    if (exception instanceof HttpException) {
      const resp = exception.getResponse() as any;
      status = exception.getStatus();
      code = resp.code || status;
      message = resp.message || exception.message;
    }

    // 记录日志
    this.logger.error(
      `请求失败：${request.method} ${request.url}`,
      exception instanceof Error ? exception.stack : '',
    );

    // 返回统一格式
    response.status(status).json({
      code,
      message,
      data: null,
    });
  }
}
```

---

## 2.7 项目结构模板

```
packages/base-backend/
├── src/
│   ├── main.ts                  # 应用入口
│   ├── app.module.ts            # 根模块
│   │
│   ├── controllers/             # 控制器层（公共）
│   │   └── health.controller.ts # 健康检查
│   │
│   ├── services/                # 服务层（公共）
│   │   └── health.service.ts
│   │
│   ├── modules/                 # 业务模块
│   │   ├── user/
│   │   │   ├── user.module.ts
│   │   │   ├── user.controller.ts
│   │   │   ├── user.service.ts
│   │   │   ├── user.dao.ts
│   │   │   ├── user.entity.ts
│   │   │   ├── user.dto.ts
│   │   │   └── index.ts
│   │   ├── role/
│   │   │   └── ...
│   │   └── auth/
│   │       └── ...
│   │
│   ├── common/                  # 公共资源
│   │   ├── decorators/          # 装饰器
│   │   │   ├── public.decorator.ts
│   │   │   └── roles.decorator.ts
│   │   ├── filters/             # 过滤器
│   │   │   └── all-exceptions.filter.ts
│   │   ├── guards/              # 守卫
│   │   │   ├── auth.guard.ts
│   │   │   └── roles.guard.ts
│   │   ├── interceptors/        # 拦截器
│   │   │   ├── logging.interceptor.ts
│   │   │   └── transform.interceptor.ts
│   │   ├── middleware/          # 中间件
│   │   │   └── logger.middleware.ts
│   │   ├── utils/               # 工具函数
│   │   │   ├── logger.ts
│   │   │   └── encrypt.ts
│   │   └── types/               # 类型定义
│   │       └── common.types.ts
│   │
│   ├── config/                  # 配置
│   │   ├── database.config.ts
│   │   ├── app.config.ts
│   │   └── index.ts
│   │
│   └── database/                # 数据库相关
│       ├── migrations/          # 迁移文件
│       └── seeds/               # 种子数据
│
├── tests/                       # 测试
│   ├── unit/
│   ├── integration/
│   └── e2e/
│
├── scripts/                     # 脚本
│   ├── migrate.ts
│   └── seed.ts
│
├── .env                         # 环境变量
├── .env.example                 # 环境变量示例
├── package.json
├── tsconfig.json
└── nest-cli.json
```

---

## 更新历史

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| 2.0.0 | 2026-03-30 | 基于业界最佳实践编写完整版 |
| 1.0.0 | 2026-03-28 | 初始版本（草案框架） |
