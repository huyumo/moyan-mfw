# 性能优化

> 状态：**已完成** | 版本：1.0.0

---

## 11.1 渲染优化

### 11.1.1 避免不必要的重新渲染

```vue
<script setup lang="ts">
import { ref, computed, memo, shallowRef } from 'vue';

// ✅ 推荐：使用 computed 缓存计算结果
const list = ref<Data[]>([]);
const filteredList = computed(() => list.value.filter(item => item.active));

// ✅ 推荐：使用 shallowRef 处理大型对象
const largeData = shallowRef<LargeObject>({});

// ❌ 避免：在模板中直接进行复杂计算
// <div>{{ list.filter(item => item.active).length }}</div>
</script>
```

### 11.1.2 列表渲染优化

```vue
<template>
  <!-- ✅ 推荐：使用唯一的 key -->
  <div v-for="item in list" :key="item.id">
    {{ item.name }}
  </div>

  <!-- ❌ 避免：使用索引作为 key -->
  <div v-for="(item, index) in list" :key="index">
    {{ item.name }}
  </div>
</template>
```

### 11.1.3 组件懒加载

```vue
<script setup lang="ts">
import { defineAsyncComponent } from 'vue';

// 异步加载大型组件
const ChartComponent = defineAsyncComponent(() =>
  import('./components/ChartComponent.vue')
);
</script>
```

---

## 11.2 代码分割

### 11.2.1 路由懒加载

```typescript
// router.ts
const routes: RouteRecordRaw[] = [
  {
    path: 'dashboard',
    name: 'Dashboard',
    // ✅ 推荐：懒加载页面组件
    component: () => import('./views/dashboard/Index.vue'),
  },
  {
    path: 'orders',
    name: 'Orders',
    component: () => import('./views/orders/Index.vue'),
  },
];
```

### 11.2.2 动态导入

```typescript
// 按功能模块分割代码
const loadModule = async (moduleName: string) => {
  const module = await import(`./modules/${moduleName}.ts`);
  return module.default;
};

// 图标组件懒加载
const icons = defineAsyncComponent(() =>
  import(/* webpackChunkName: "icons" */ './components/Icons.vue')
);
```

### 11.2.3 第三方库按需加载

```typescript
// ✅ 推荐：按需导入 Element Plus 组件
import { ElButton, ElTable } from 'element-plus';

// ❌ 避免：导入整个库
// import ElementPlus from 'element-plus';

// ✅ 推荐：lodash 按需导入
import { debounce } from 'lodash-es';

// ❌ 避免：导入整个 lodash
// import _ from 'lodash';
```

---

## 11.3 缓存策略

### 11.3.1 keep-alive 缓存

```vue
<template>
  <router-view v-slot="{ Component, route }">
    <keep-alive :include="['UserList', 'OrderList']">
      <component :is="Component" :key="route.fullPath" />
    </keep-alive>
  </router-view>
</template>
```

### 11.3.2 API 响应缓存

```typescript
// composables/useCachedApi.ts
import { ref } from 'vue';

const cache = new Map<string, { data: unknown; timestamp: number }>();
const CACHE_TTL = 5 * 60 * 1000; // 5 分钟

export function useCachedApi<T>(
  key: string,
  fetcher: () => Promise<T>
): Promise<T> {
  const cached = cache.get(key);
  if (cached && Date.now() - cached.timestamp < CACHE_TTL) {
    return Promise.resolve(cached.data as T);
  }

  return fetcher().then((data) => {
    cache.set(key, { data, timestamp: Date.now() });
    return data;
  });
}
```

---

## 11.4 资源优化

### 11.4.1 图片优化

```vue
<template>
  <!-- ✅ 推荐：使用 WebP 格式 -->
  <img src="image.webp" alt="描述" />

  <!-- ✅ 推荐：响应式图片 -->
  <img
    srcset="image-320w.jpg 320w,
            image-480w.jpg 480w,
            image-800w.jpg 800w"
    sizes="(max-width: 320px) 280px,
           (max-width: 480px) 440px,
           800px"
    src="image-800w.jpg"
    alt="描述"
  />

  <!-- ✅ 推荐：懒加载图片 -->
  <img src="image.jpg" loading="lazy" alt="描述" />
</template>
```

### 11.4.2 字体优化

```scss
// ✅ 推荐：使用 font-display: swap
@font-face {
  font-family: 'CustomFont';
  src: url('/fonts/custom.woff2') format('woff2');
  font-display: swap;
}
```

---

## 11.5 性能监控

### 11.5.1 核心性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| FCP (First Contentful Paint) | < 1.5s | 首次内容绘制 |
| LCP (Largest Contentful Paint) | < 2.5s | 最大内容绘制 |
| TTI (Time to Interactive) | < 3.5s | 可交互时间 |
| CLS (Cumulative Layout Shift) | < 0.1 | 累积布局偏移 |

### 11.5.2 性能分析

```typescript
// 性能监控
onMounted(() => {
  // 获取 Web Vitals 指标
  if ('PerformanceObserver' in window) {
    const observer = new PerformanceObserver((list) => {
      for (const entry of list.getEntries()) {
        console.log('Performance:', entry.name, entry.startTime);
      }
    });
    observer.observe({ entryTypes: ['paint', 'largest-contentful-paint'] });
  }
});
```

---

## 更新历史

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| 1.0.0 | 2026-03-29 | 初始版本，定义渲染优化、代码分割、缓存策略规范 |
