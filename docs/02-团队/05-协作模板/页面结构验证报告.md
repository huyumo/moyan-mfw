# 页面结构验证报告

> 模板版本：v1.0 | 创建日期：2026-04-02

---

## 验证信息

| 项目 | 内容 |
|------|------|
| 验证编号 | `[编号]`（如 PAGE-2026-001） |
| 验证日期 | `[日期]` |
| 验证人 | `[姓名/角色]` |
| 页面路径 | `[路由路径]` |
| 页面组件 | `[组件路径]` |

---

## 页面结构概览

### 主要元素

| 元素类型 | 选择器 | data-testid | 备注 |
|----------|--------|-------------|------|
| 页面容器 | `[选择器]` | `[testid]` | - |
| 搜索表单 | `[选择器]` | `[testid]` | - |
| 数据表格 | `[选择器]` | `[testid]` | - |
| 操作按钮 | `[选择器]` | `[testid]` | - |
| 弹窗/对话框 | `[选择器]` | `[testid]` | - |

---

## 关键交互元素清单

### 搜索区域

| 元素 | 选择器 | 稳定性 | 建议 |
|------|--------|--------|------|
| 搜索按钮 | `getByRole('button', { name: '搜索' })` | ✅ 稳定 | - |
| 重置按钮 | `getByRole('button', { name: '重置' })` | ✅ 稳定 | - |
| 输入框 | `getByRole('textbox', { name: '[字段名]' })` | ✅ 稳定 | - |

### 表格区域

| 元素 | 选择器 | 稳定性 | 建议 |
|------|--------|--------|------|
| 表格 | `getByRole('table')` | ✅ 稳定 | - |
| 表格行 | `[选择器]` | ⚠️ 不稳定 | 建议添加 data-testid |
| 操作按钮 | `getByRole('button', { name: '编辑' })` | ✅ 稳定 | - |

### 弹窗区域

| 元素 | 选择器 | 稳定性 | 建议 |
|------|--------|--------|------|
| 弹窗容器 | `getByRole('dialog')` | ✅ 稳定 | - |
| 确认按钮 | `getByRole('button', { name: '确定' })` | ✅ 稳定 | - |
| 取消按钮 | `getByRole('button', { name: '取消' })` | ✅ 稳定 | - |

---

## 验证方法

### 使用工具

- [ ] Playwright codegen
- [ ] Grep 工具搜索组件源码
- [ ] 手动检查 DOM 结构

### 验证步骤

```
1. 启动前端服务：pnpm run dev:frontend
2. 启动 Playwright codegen：npx playwright codegen http://localhost:5174/[路径]
3. 操作页面，记录生成的选择器
4. 对比组件源码确认选择器正确性
5. 记录不稳定选择器，建议添加 data-testid
```

---

## 问题与建议

### 缺失 data-testid 的元素

| 元素 | 当前选择器 | 建议 testid |
|------|------------|-------------|
| `[元素描述]` | `[不稳定选择器]` | `[建议 testid]` |

### 选择器冲突问题

| 问题描述 | 影响范围 | 解决方案 |
|----------|----------|----------|
| `[问题描述]` | `[影响]` | `[方案]` |

---

## API 可用性验证

| API | 状态 | 响应格式 | 备注 |
|-----|------|----------|------|
| `GET /api/v1/[接口]` | ✅ 可用 | 符合文档 | - |
| `POST /api/v1/[接口]` | ❌ 不可用 | - | 需后端确认 |

---

## 验收标准

- [ ] 所有关键元素选择器已确认
- [ ] 不稳定选择器已记录建议
- [ ] API 可用性已验证
- [ ] 测试用例可基于此报告编写

---

**签字确认**：

| 角色 | 签字 | 日期 |
|------|------|------|
| 测试工程师 | - | - |
| 前端开发 | - | - |